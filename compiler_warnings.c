/*
DO NOT IGNORE COMPILER WARNINGS

Recommended to use -w all to show all compiler warnings because you want to fix all of them

Compiler warnings are not errors, but they indicate potential issues in your code.
They can help you catch mistakes before they become runtime errors.

It's only a warning because the compiler is still able to generate correct machine code

int total;
printf("%d\n", total);

warning: 'total' is used uninitialized in this function

*/

